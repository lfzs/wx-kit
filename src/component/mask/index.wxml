<view class="mask {{ show && 'active' }} {{ position }}" catchtouchmove="noop" bindtap="onMask">
  <view class="content {{ show && 'show-content' }}" catchtap="noop" catchtouchmove="noop">
    <slot />
  </view>
</view>
